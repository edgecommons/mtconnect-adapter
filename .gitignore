/target
/greengrass-build
*.metric.log
# LOCAL DEV ONLY — the pinned-rev .cargo/config.toml patch override points at your sibling
# checkout and must not be committed to your component repo.
/.cargo/
# Cargo.lock is committed for binary components; the template ships without one so
# the first build generates it for your crate.

# Release-leg run output. The harness under host-leg/ regenerates these; the captured
# evidence belongs in the release record, not in the source tree.
/host-leg/evidence/
/host-leg/target/
/k8s-leg/target/
/k8s-leg/*control.txt
/lab-leg/ipc-sniffer/target/
