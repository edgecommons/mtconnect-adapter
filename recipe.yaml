RecipeFormatVersion: '2020-01-25'
ComponentName: com.mbreissi.edgecommons.MtconnectAdapter
ComponentVersion: 1.0.0
ComponentDescription: The MtconnectAdapter component.
ComponentPublisher: 
ComponentConfiguration:
  DefaultConfiguration:
    ComponentConfig:
      logging:
        level: INFO
        rust_format: '{timestamp} [{level}] [{component}] {target} - {message}'
      heartbeat:
        enabled: true
        intervalSecs: 5
        destination: local
        measures:
          cpu: true
          memory: true
          disk: false
      metricEmission:
        target: log
        namespace: edgecommons
        targetConfig:
          logFileName: /greengrass/v2/work/{ComponentFullName}/metric.log
      tags:
        appId: Test
        site: Chantilly
      component:
        # UNS component token. The Greengrass ComponentName is reverse-DNS PascalCase; the token the
        # wire carries is lower-kebab, so it is set explicitly rather than derived from the name.
        token: mtconnect-adapter
        global:
          defaults:
            pollIntervalMs: 5000
          timeouts:
            connectMs: 5000
            reconnectBackoffMinMs: 1000
            reconnectBackoffMaxMs: 60000
          healthThresholds:
            staleSignalSecs: 30
        instances:
        - id: device-1
          adapter: sim
          connection:
            endpoint: sim://device-1
          pollIntervalMs: 5000
          writes:
            allow: []
    accessControl:
      aws.greengrass.ipc.pubsub:
        com.mbreissi.edgecommons.MtconnectAdapter:pubsub:1:
          policyDescription: Allows access to publish/subscribe to all local topics.
          operations:
          - aws.greengrass#PublishToTopic
          - aws.greengrass#SubscribeToTopic
          resources:
          - '*'
      aws.greengrass.ipc.mqttproxy:
        com.mbreissi.edgecommons.MtconnectAdapter:northbound:1:
          policyDescription: Allows access to publish/subscribe to all topics on IoT Core.
          operations:
          - aws.greengrass#PublishToIoTCore
          - aws.greengrass#SubscribeToIoTCore
          resources:
          - '*'
      aws.greengrass.ShadowManager:
        com.mbreissi.edgecommons.MtconnectAdapter:shadow:1:
          policyDescription: Allows access to thing shadows.
          operations:
          - aws.greengrass#GetThingShadow
          - aws.greengrass#UpdateThingShadow
          - aws.greengrass#DeleteThingShadow
          resources:
          - '*'
Manifests:
- Platform:
    os: linux
  Artifacts:
  - URI: s3://BUCKET_NAME/COMPONENT_NAME/COMPONENT_VERSION/mtconnect-adapter
  Lifecycle:
    Install:
      Script: chmod +x {artifacts:path}/mtconnect-adapter
    Run:
      Script: '{artifacts:path}/mtconnect-adapter --platform GREENGRASS -c GG_CONFIG'
